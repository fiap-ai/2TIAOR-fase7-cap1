meta {
  name: Login
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "username": "admin",
    "password": "cardioai"
  }
}

assert {
  res.status: eq 200
  res.body.token: isDefined
  res.body.user.name: eq Dr. Admin
}
