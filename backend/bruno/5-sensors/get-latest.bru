meta {
  name: Get Latest Readings
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/sensors/latest?limit=10
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.readings: isDefined
  res.body.count: isDefined
}
