meta {
  name: Ingest Sensor Data
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/sensors
  body: json
  auth: none
}

body:json {
  {
    "temperature": 36.8,
    "heart_rate": 75,
    "device_id": "esp32-01"
  }
}

assert {
  res.status: eq 200
  res.body.status: eq received
}
